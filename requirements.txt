# Web framework — receives WhatsApp webhooks from Meta
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Async HTTP client — calls the WhatsApp Cloud API (Meta Graph API)
httpx>=0.27.0

# OpenAI API client
openai>=1.56.0

# Google BigQuery client
google-cloud-bigquery==3.25.0

# Loads .env files into environment variables
python-dotenv==1.0.1

# Chart generation
matplotlib>=3.8.0
numpy>=1.26.0
squarify>=0.4.3

# Google Sheets audit logging
gspread>=6.0.0

# Date arithmetic for completeness query detection
python-dateutil>=2.8.0

# Export generation (Excel, PowerPoint, image sizing)
openpyxl>=3.1.0
python-pptx>=0.6.21
Pillow>=9.0.0
